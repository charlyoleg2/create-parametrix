{{RepoName}}
{{{RepoNameUnderline}}}


Presentation
------------

*{{RepoName}}* is the top-monorepo for the design-library *{{libName}}*, which contains a collection of 3D shapes.

This monorepo contains the following *javascript* package:

1. {{libName}}: a *parametrix* design library
2. {{libName}}-cli: the cli of {{libName}}
3. {{libName}}-ui: the web-ui of {{libName}}
4. {{libName}}-uis: the web-server of {{libName}}-ui

This repo is a typical designer-repository using [parametrix](https://charlyoleg2.github.io/parametrix/).
The design-library and its associated UI and CLI are published as *npm-packages*.
The UI is also available on the github-page.


Links
-----

- [{{libName}}-ui](https://MYNAME.github.io/{{repoName}}/) : public instance of the UI
- [sources](https://github.com/MYNAME/{{repoName}}) : git-repository
- [pkg](https://www.npmjs.com/package/{{libName}}) : {{libName}} as npm-package
- [pkg-cli](https://www.npmjs.com/package/{{libName}}-cli) : {{libName}}-cli as npm-package
- [pkg-uis](https://www.npmjs.com/package/{{libName}}-uis) : {{libName}}-uis as npm-package


Usage for Makers
----------------

Parametrize and generate your 3D-files with the online-app:

[https://MYNAME.github.io/{{repoName}}/](https://MYNAME.github.io/{{repoName}}/)

Or use the UI locally:

```bash
npx {{libName}}-uis
```

Or use the command-line-interface (CLI):

```bash
npx {{libName}}-cli
```

Getting started for Dev
-----------------------

```bash
git clone https://github.com/MYNAME/{{repoName}}
cd {{repoName}}
npm i
npm run ci
npm run preview
```

Other useful commands:
```bash
npm run clean
npm run ls-workspaces
npm -w {{libName}} run check
npm -w {{libName}} run build
npm -w {{libName}}-ui run dev
```

Prerequisite
------------

- [node](https://nodejs.org) version 22.0.0 or higher
- [npm](https://docs.npmjs.com/cli/v11/commands/npm) version 11.0.0 or higher


Publish a new release
---------------------

```bash
npm run versions
git commit -am 'increment sub versions'
npm version patch
git push
git push origin v0.5.6
```
