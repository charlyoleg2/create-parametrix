import { describe, it, expect } from 'vitest';
//import { serve } from './{{libName}}-uis';

describe('{{libName}}-uis suit', () => {
	it('{{libName}}-uis execution', () => {
		expect(2 + 2).toBe(4);
	});
});
