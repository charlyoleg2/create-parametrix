import { describe, it, expect } from 'vitest';
//import { app-cli } from './{{libName}}-cli';

describe('{{libName}}-cli suit', () => {
	it('app-cli execution', () => {
		expect(2 + 2).toBe(4);
	});
});
